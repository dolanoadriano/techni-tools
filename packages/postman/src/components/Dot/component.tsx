import React from "react";

import "./style.scss"
import { Props } from "./types";
    
const Dot: React.FC<Props> = (props) => {
  const {  } = props;
  return (
    <div className={`Dot`}>
      
    </div>
  );
};
    
export default Dot;